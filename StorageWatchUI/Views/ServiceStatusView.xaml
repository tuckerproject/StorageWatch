<UserControl x:Class="StorageWatchUI.Views.ServiceStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Service Status" FontSize="24" FontWeight="Bold" 
                   Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,20"/>

        <!-- Service Status Card -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Service: " FontSize="16" 
                               Foreground="{DynamicResource SecondaryText}"/>
                    <TextBlock Text="StorageWatchService" FontSize="16" FontWeight="Bold" 
                               Foreground="{DynamicResource PrimaryText}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Status: " FontSize="16" 
                               Foreground="{DynamicResource SecondaryText}"/>
                    <TextBlock Text="{Binding ServiceStatus}" FontSize="16" FontWeight="Bold" 
                               Foreground="{DynamicResource PrimaryText}"/>
                    <TextBlock Text=" (Not Installed)" FontSize="14" 
                               Foreground="Orange"
                               Margin="5,0,0,0"
                               Visibility="{Binding IsServiceInstalled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Start Service" Command="{Binding StartServiceCommand}" 
                            Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="Stop Service" Command="{Binding StopServiceCommand}" 
                            Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="Restart Service" Command="{Binding RestartServiceCommand}" 
                            Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                            Padding="10,5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Log Header -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Recent Log Entries (Last 20)" FontSize="16" FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center"/>
            <Button Content="Refresh Logs" Command="{Binding RefreshLogsCommand}" 
                    Padding="8,4" Margin="15,0,0,0"/>
        </StackPanel>

        <!-- Log Display -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding RecentLogs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" 
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       Foreground="{DynamicResource PrimaryText}"
                                       Margin="0,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
