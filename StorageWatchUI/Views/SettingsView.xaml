<UserControl x:Class="StorageWatchUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Settings" FontSize="24" FontWeight="Bold" 
                   Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Open Config in Notepad" Command="{Binding OpenConfigCommand}" 
                    Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Test Alerts" Command="{Binding TestAlertsCommand}" 
                    Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                    Padding="10,5"/>
        </StackPanel>

        <!-- Configuration Display -->
        <Border Grid.Row="2" Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding ConfigurationJson, Mode=OneWay}" 
                         IsReadOnly="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{DynamicResource PrimaryText}"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
