<Window x:Class="StorageWatchUI.Views.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Update Available" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Stretch">
            <!-- Title -->
            <TextBlock Text="StorageWatch Update" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#FF333333"
                       Margin="0,0,0,15"/>

            <!-- Current and New Version -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Current Version" FontSize="12" Foreground="#FF666666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding CurrentVersion}" FontSize="16" FontWeight="Bold" Foreground="#FF333333"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="New Version" FontSize="12" Foreground="#FF666666" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding NewVersion}" FontSize="16" FontWeight="Bold" Foreground="#FF4CAF50"/>
                </StackPanel>
            </Grid>

            <!-- Release Notes -->
            <TextBlock Text="Release Notes:" FontSize="12" Foreground="#FF666666" Margin="0,0,0,5"/>
            <ScrollViewer Height="150" Margin="0,0,0,15" Background="#FFF5F5F5" Padding="10">
                <TextBlock Text="{Binding ReleaseNotes}" 
                           TextWrapping="Wrap" 
                           Foreground="#FF333333"
                           FontSize="11"/>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancel" 
                        Command="{Binding CancelCommand}"
                        Width="100" 
                        Padding="10,8"
                        Margin="0,0,10,0"
                        Background="Transparent"
                        Foreground="#FF333333"
                        BorderThickness="1"
                        BorderBrush="#FFCCCCCC"
                        Cursor="Hand"/>
                <Button Content="Update" 
                        Command="{Binding UpdateCommand}"
                        Width="100" 
                        Padding="10,8"
                        Background="#FF4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
