# StorageWatchAgent Alert Sender Plugin Architecture Diagram

## Component Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         StorageWatchAgent                              │
│                                                                           │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                          Worker.cs                                 │  │
│  │  ┌──────────────────────────────────────────────────────────────┐ │  │
│  │  │              NotificationLoop                                 │ │  │
│  │  │  - Monitors disk space                                        │ │  │
│  │  │  - Detects state changes                                      │ │  │
│  │  │  - Calls SendAlertAsync(DiskStatus, CancellationToken)        │ │  │
│  │  └──────────────────────┬───────────────────────────────────────┘ │  │
│  └─────────────────────────┼─────────────────────────────────────────┘  │
│                            │                                             │
│                            ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              List<IAlertSender> (from Plugin Manager)            │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │   │
│  │  │  SMTP    │  │ GroupMe  │  │  Slack   │  │  Teams   │  ...   │   │
│  │  │  Plugin  │  │  Plugin  │  │  Plugin  │  │  Plugin  │        │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

## Plugin Architecture Layers

```
┌────────────────────────────────────────────────────────────────────────┐
│                         Application Layer                               │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  Worker Service                                                   │  │
│  │  - Uses AlertSenderPluginManager via DI                           │  │
│  │  - Gets enabled senders                                           │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬─────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────────┐
│                       Plugin Management Layer                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  AlertSenderPluginManager                                         │  │
│  │  - GetEnabledSenders() → List<IAlertSender>                       │  │
│  │  - PerformHealthChecksAsync() → Dictionary<string, bool>          │  │
│  │  - GetPluginInfo() → List<AlertSenderPluginMetadata>              │  │
│  └────────────┬─────────────────────────────────┬───────────────────┘  │
│               │                                 │                       │
│               ▼                                 ▼                       │
│  ┌────────────────────────┐      ┌────────────────────────────────┐   │
│  │  AlertSenderPlugin     │      │  IServiceProvider              │   │
│  │  Registry              │      │  (DI Container)                │   │
│  │  - DiscoverPlugins()   │      │  - Resolves IAlertSender       │   │
│  │  - GetPlugin(id)       │      │    implementations             │   │
│  └────────────────────────┘      └────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────────┐
│                         Plugin Interface Layer                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  IAlertSender (Interface)                                         │  │
│  │  + string Name { get; }                                           │  │
│  │  + Task SendAlertAsync(DiskStatus, CancellationToken)             │  │
│  │  + Task<bool> HealthCheckAsync(CancellationToken)                 │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                               ▲                                         │
│                               │                                         │
│  ┌────────────────────────────┴────────────────────────────────────┐  │
│  │  AlertSenderBase (Abstract Base Class)                           │  │
│  │  - Common message formatting                                     │  │
│  │  - Error handling                                                │  │
│  │  - Logging infrastructure                                        │  │
│  │  # abstract bool IsEnabled()                                     │  │
│  │  # abstract Task SendMessageAsync(string, CancellationToken)     │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────────────────┐
│                      Plugin Implementation Layer                        │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐           │
│  │ SmtpAlert      │  │ GroupMeAlert   │  │ SlackAlert     │  (Future) │
│  │ Sender         │  │ Sender         │  │ Sender         │           │
│  │ [Plugin]       │  │ [Plugin]       │  │ [Plugin]       │           │
│  │ - SMTP logic   │  │ - GroupMe API  │  │ - Slack API    │           │
│  └────────────────┘  └────────────────┘  └────────────────┘           │
│                                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐           │
│  │ TeamsAlert     │  │ DiscordAlert   │  │ WebhookAlert   │  (Future) │
│  │ Sender         │  │ Sender         │  │ Sender         │           │
│  │ [Plugin]       │  │ [Plugin]       │  │ [Plugin]       │           │
│  └────────────────┘  └────────────────┘  └────────────────┘           │
└────────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Sending an Alert

```
1. NotificationLoop detects disk space issue
   │
   ├─> Creates DiskStatus object
   │   { DriveName: "C:", TotalSpaceGb: 100, FreeSpaceGb: 5 }
   │
   └─> Iterates through List<IAlertSender> senders
       │
       ├─> sender.SendAlertAsync(diskStatus, cancellationToken)
       │   │
       │   └─> [Plugin: SMTP]
       │       ├─> Check IsEnabled() → true
       │       ├─> FormatMessage(diskStatus) → "ALERT — SERVER: Drive C: is below threshold..."
       │       ├─> SendMessageAsync(message) → SMTP email sent
       │       └─> Log success/failure
       │
       ├─> sender.SendAlertAsync(diskStatus, cancellationToken)
       │   │
       │   └─> [Plugin: GroupMe]
       │       ├─> Check IsEnabled() → true
       │       ├─> FormatMessage(diskStatus) → "ALERT — SERVER: Drive C: is below threshold..."
       │       ├─> SendMessageAsync(message) → HTTP POST to GroupMe API
       │       └─> Log success/failure
       │
       └─> sender.SendAlertAsync(diskStatus, cancellationToken)
           │
           └─> [Plugin: Slack] (if enabled)
               ├─> Check IsEnabled() → true
               ├─> FormatMessage(diskStatus) → "🚨 ALERT — SERVER: Drive C: ..."
               ├─> SendMessageAsync(message) → HTTP POST to Slack webhook
               └─> Log success/failure
```

## Configuration Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│  StorageWatchConfig.json                                             │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  "Alerting": {                                                 │  │
│  │    "EnableNotifications": true,                                │  │
│  │    "Smtp": { "Enabled": true, ... },                           │  │
│  │    "GroupMe": { "Enabled": false, ... },                       │  │
│  │    "Plugins": {                                                │  │
│  │      "Slack": { "Enabled": true, "WebhookUrl": "..." }         │  │
│  │    }                                                            │  │
│  │  }                                                              │  │
│  └───────────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  JsonConfigLoader                                                    │
│  - Loads and validates JSON                                         │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  StorageWatchOptions (strongly-typed)                                │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  AlertingOptions                                               │  │
│  │    ├─ EnableNotifications: true                                │  │
│  │    ├─ Smtp: SmtpOptions { Enabled: true, ... }                 │  │
│  │    ├─ GroupMe: GroupMeOptions { Enabled: false, ... }          │  │
│  │    └─ Plugins: Dictionary<string, Dictionary<string, object>>  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  StorageWatchOptionsValidator                                        │
│  - Validates at least one sender enabled when notifications = true  │
│  - Checks both legacy and new plugin configs                        │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  AlertSenderPluginManager                                            │
│  - Reads EnableNotifications flag                                   │
│  - Checks legacy config: Smtp.Enabled, GroupMe.Enabled              │
│  - Checks new config: Plugins["PluginId"]["Enabled"]                │
│  - Returns filtered list of enabled IAlertSender instances          │
└─────────────────────────────────────────────────────────────────────┘
```

## Plugin Registration Flow (DI)

```
Program.cs startup
│
├─> Create AlertSenderPluginRegistry
│   └─> registry.DiscoverPlugins()
│       ├─> Scan assembly for types implementing IAlertSender
│       ├─> Find SmtpAlertSender [AlertSenderPlugin("SMTP")]
│       ├─> Find GroupMeAlertSender [AlertSenderPlugin("GroupMe")]
│       └─> Create metadata for each plugin
│
├─> Register plugins in DI container
│   ├─> services.AddTransient<SmtpAlertSender>
│   ├─> services.AddTransient<GroupMeAlertSender>
│   ├─> services.AddTransient<IAlertSender, SmtpAlertSender>
│   └─> services.AddTransient<IAlertSender, GroupMeAlertSender>
│
├─> Register AlertSenderPluginManager as singleton
│   └─> Depends on: IServiceProvider, StorageWatchOptions, RollingFileLogger
│
└─> Worker service starts
    ├─> Injects IServiceProvider
    ├─> Creates AlertSenderPluginManager
    └─> Calls pluginManager.GetEnabledSenders()
        ├─> Resolves all IAlertSender from DI
        ├─> Filters based on configuration
        └─> Returns List<IAlertSender> to NotificationLoop
```

## Health Check Flow

```
Application startup or periodic check
│
└─> AlertSenderPluginManager.PerformHealthChecksAsync()
    │
    ├─> Get all enabled senders
    │   └─> [SMTP, GroupMe, Slack]
    │
    ├─> For each sender:
    │   ├─> await sender.HealthCheckAsync()
    │   │   │
    │   │   └─> [SMTP]
    │   │       ├─> Check IsEnabled() → true
    │   │       ├─> Validate Host not empty → true
    │   │       ├─> Validate FromAddress not empty → true
    │   │       └─> Return true (healthy)
    │   │
    │   ├─> await sender.HealthCheckAsync()
    │   │   │
    │   │   └─> [GroupMe]
    │   │       ├─> Check IsEnabled() → true
    │   │       ├─> Validate BotId not empty → true
    │   │       └─> Return true (healthy)
    │   │
    │   └─> await sender.HealthCheckAsync()
    │       │
    │       └─> [Slack]
    │           ├─> Check IsEnabled() → true
    │           ├─> Validate WebhookUrl not empty → false
    │           └─> Return false (unhealthy)
    │
    └─> Return Dictionary<string, bool>
        {
          "SMTP": true,
          "GroupMe": true,
          "Slack": false
        }
```

## Extension Point: Adding New Plugin

```
Developer creates new plugin
│
├─> 1. Create Options class
│   └─> public class TeamsOptions { ... }
│
├─> 2. Implement IAlertSender (or inherit AlertSenderBase)
│   └─> [AlertSenderPlugin("Teams", ...)]
│       public class TeamsAlertSender : AlertSenderBase { ... }
│
├─> 3. Add to AlertingOptions
│   └─> public TeamsOptions Teams { get; set; } = new();
│
├─> 4. Register in Program.cs
│   ├─> services.AddTransient<TeamsAlertSender>
│   └─> services.AddTransient<IAlertSender, TeamsAlertSender>
│
└─> 5. Configure in JSON
    └─> "Alerting": { "Teams": { "Enabled": true, ... } }

Plugin is automatically discovered and used!
```

---

## Legend

```
┌─────┐
│ Box │  Component or layer
└─────┘

  │
  │     Flow direction
  ▼

┌─────────────┐
│ [Plugin]    │  Plugin implementation
└─────────────┘

  →     Data flow
  ├─>   Branching flow
  └─>   End of branch
```

---

This architecture provides:
- ✅ Clean separation of concerns
- ✅ Easy extensibility
- ✅ Testability
- ✅ DI integration
- ✅ Configuration flexibility
- ✅ Health monitoring
- ✅ Backward compatibility
